# TODO

Use this list for next actions and cross-PC continuity.

## Immediate

- [ ] Confirm VS Code `Python: Select Interpreter` is `.venv\Scripts\python.exe` on each PC.
- [ ] Remove stale env folders if present (`.venv-1`, `.venv-2`, `.venv-3`).
- [ ] Run `setup.ps1` once on each PC after sync.
- [ ] Verify `Run NHL Cues (prompt Game ID)` task works on each PC.

## Nice to have

- [ ] Add `.gitignore` entries for `.venv*` and other local artifacts.
- [ ] Add a short `SYNC.md` with “before switching PC” checklist.
- [ ] Add a task for preset run modes (`--from-start`, `--send-hotkey`).

## Done recently

- [x] Created beginner `RUNBOOK.md`.
- [x] Added VS Code task(s) to run in integrated terminal.
- [x] Added `setup.ps1` one-command environment bootstrap.
